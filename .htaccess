# AquaDT — корень после переноса: лендинг + 301 на /old/

<Files ".env">
  Require all denied
</Files>

AddDefaultCharset UTF-8
Options -Indexes
DirectoryIndex index.html index.php

RewriteEngine On
RewriteBase /

# HTTPS
RewriteCond %{HTTPS} off
RewriteRule ^(.*)$ https://%{HTTP_HOST}%{REQUEST_URI} [R=301,L]

# www → без www
RewriteCond %{HTTP_HOST} ^www\.(.+)$ [NC]
RewriteRule ^(.*)$ https://%1/$1 [R=301,L]

# Убрать завершающий слэш (кроме корня и реальных директорий)
RewriteCond %{REQUEST_FILENAME} !-d
RewriteCond %{REQUEST_URI} ^(.+)/$
RewriteRule ^(.+)/$ /$1 [R=301,L]

# Старый URL лендинга → новая главная
RewriteRule ^landing/?$ / [R=301,L]

# Постраничные 301: старые ЧПУ → /old/...
RewriteRule ^(production)/?$ /old/$1 [R=301,L]
RewriteRule ^(pseudo-sea)/?$ /old/$1 [R=301,L]
RewriteRule ^(sea)/?$ /old/$1 [R=301,L]
RewriteRule ^(nature)/?$ /old/$1 [R=301,L]
RewriteRule ^(japan)/?$ /old/$1 [R=301,L]
RewriteRule ^(form)/?$ /old/$1 [R=301,L]
RewriteRule ^(payment)/?$ /old/$1 [R=301,L]
RewriteRule ^(decoration)/?$ /old/$1 [R=301,L]
RewriteRule ^(ponds)/?$ /old/$1 [R=301,L]
RewriteRule ^(hardscape)/?$ /old/$1 [R=301,L]
RewriteRule ^(service)/?$ /old/$1 [R=301,L]
RewriteRule ^(stabilized-moss)/?$ /old/$1 [R=301,L]
RewriteRule ^(thanks)/?$ /old/$1 [R=301,L]

# Ресурсы и служебные пути старого сайта
RewriteRule ^resource/(.*)$ /old/resource/$1 [R=301,L]
RewriteRule ^admin/(.*)$ /old/admin/$1 [R=301,L]
RewriteRule ^install/(.*)$ /old/install/$1 [R=301,L]

# Существующие файлы/папки лендинга и /old/ — без перезаписи
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]
